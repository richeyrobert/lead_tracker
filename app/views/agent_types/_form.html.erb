<%= form_for(@agent_type) do |f| %>
  <% if @agent_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@agent_type.errors.count, "error") %> prohibited this agent_type from being saved:</h2>

      <ul>
      <% @agent_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %><br />
    <%= f.text_field :type %>
  </div>
  <div class="field">
    <%= f.label :notes %><br />
    <%= f.text_area :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
