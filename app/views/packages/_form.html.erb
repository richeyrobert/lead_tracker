<%= form_for(@package) do |f| %>
  <% if @package.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@package.errors.count, "error") %> prohibited this package from being saved:</h2>

      <ul>
      <% @package.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :package_status %><br />
    <%#= f.text_field :package_status %>
    <%#= f.collection_select(:lead_source_id, LeadSource.all, :id, :source, :prompt => "Select a Source") %>
    <%= f.collection_select  :package_status_id, PackageStatus.all, :id, :status, :prompt => "Select a Status"  %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :price %><br />
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :commission_partner %><br />
    <%#= f.text_field :commission_partner %>
    <%= f.collection_select  :commission_partner_id, Partner.all, :id, :name, :prompt => "Select a Partner"  %>
  </div>
  <div class="field">
    <%= f.label :commission_agent %><br />
    <%#= f.text_field :commission_agent %>
    <%= f.collection_select  :commission_agent_id, Agent.all, :id, :name, :prompt => "Select an Agent"  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
