<%= form_for(@installer) do |f| %>
  <% if @installer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@installer.errors.count, "error") %> prohibited this installer from being saved:</h2>

      <ul>
      <% @installer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :phone %><br />
    <%= f.text_field :phone %>
  </div>
  <div class="field">
    <%= f.label :installer_status %><br />
    <%= f.text_field :installer_status %>
  </div>
  <div class="field">
    <%= f.label :notes %><br />
    <%= f.text_area :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
