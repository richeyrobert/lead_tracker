<%= form_for(@lead_step) do |f| %>
  <% if @lead_step.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lead_step.errors.count, "error") %> prohibited this lead_step from being saved:</h2>

      <ul>
      <% @lead_step.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :step %><br />
    <%= f.text_field :step %>
  </div>
  <div class="field">
    <%= f.label :notes %><br />
    <%= f.text_area :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
